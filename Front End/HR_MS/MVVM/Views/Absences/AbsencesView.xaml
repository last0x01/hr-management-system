<UserControl x:Class="HR_MS.MVVM.Views.Absences.AbsencesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HR_MS.MVVM.Views.Absences"
             mc:Ignorable="d" 
           xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:HR_MS.MVVM.ViewModels.Absences"
             d:DesignHeight="450" d:DesignWidth="800">



    <StackPanel>
        <TextBlock Text="Absences"  Foreground="#537188" FontSize="30" FontWeight="Bold"
                   DockPanel.Dock="Top" TextAlignment="Center" Margin="0,0,0,20"/>

        <Grid Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Export Today (Left aligned) -->
            <Button Grid.Column="0" Command="{Binding ExportTodayCommand}" Style="{StaticResource PrimaryButton}">
                <StackPanel Orientation="Horizontal">
                    <icon:PackIconMaterial Kind="FileExport" Style="{StaticResource menuButtonIcon}" Margin="0,5"/>
                    <TextBlock Text=" Export Today" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Spacer in middle -->
            <StackPanel Grid.Column="1"/>

            <Button Grid.Column="2" Command="{Binding AddAbsenceCommand}" Style="{StaticResource PrimaryButton}">
                <StackPanel Orientation="Horizontal">
                    <icon:PackIconMaterial Kind="Plus" Style="{StaticResource menuButtonIcon}" Margin="0,5"/>
                    <TextBlock Text=" Add Absence"/>
                </StackPanel>
            </Button>
        </Grid>

        <DataGrid SelectedItem="{Binding SelectedAbsence, Mode=TwoWay}" 
                  ItemsSource="{Binding Absences}" 
                  Style="{DynamicResource DataGridStyle1}" 
                  CellStyle="{DynamicResource DataGridCellStyle1}" 
                  IsReadOnly="True"
                  RowStyle="{DynamicResource DataGridRowStyle1}" 
                  AutoGenerateColumns="False" MaxHeight="380">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Absence ID" Binding="{Binding AbsenceID}" />
                <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeID}" />
                <DataGridTextColumn Header="Employee Name" Binding="{Binding EmployeeName}" Width="150"/>
                <DataGridTextColumn Header="Date" Binding="{Binding AbsenceDate, StringFormat=d}" />
                <DataGridTextColumn Header="Type" Binding="{Binding AbsenceTypeName}" />
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="200"/>
                <DataGridTextColumn Header="Created By" Binding="{Binding CreatedByUserID}" />

                <DataGridTemplateColumn Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding DataContext.EditAbsenceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Style="{StaticResource actionButton}" Background="#3a9bdc">
                                    <icon:PackIconMaterial Kind="AccountEdit" Style="{StaticResource actionButtonIcon}"/>
                                </Button>
                               
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>