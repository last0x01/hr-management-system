<UserControl x:Class="HR_MS.MVVM.Views.Attendances.AttendancesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HR_MS.MVVM.Views.Attendances"
             xmlns:vm="clr-namespace:HR_MS.MVVM.ViewModels.Attendances"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:AttendancesViewModel/>
        </UserControl.DataContext>

            <StackPanel>
        <TextBlock Text="Attendances" Foreground="#537188"  FontSize="30" FontWeight="Bold"
                                DockPanel.Dock="Top" TextAlignment="Center" />

        <Grid Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Add New Attendance (Right aligned) -->
            <Button Grid.Column="1"
            Style="{StaticResource pagingButton}">
                <StackPanel Orientation="Horizontal">
                    <icon:PackIconMaterial Kind="Plus"
                                   Style="{StaticResource menuButtonIcon}"
                                   Margin="0,5"/>
                    <TextBlock Text=" Add Attendance"
                       VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <DataGrid Style="{DynamicResource DataGridStyle1}"
          CellStyle="{DynamicResource DataGridCellStyle1}"
          RowStyle="{DynamicResource DataGridRowStyle1}"
          IsReadOnly="True"
          ItemsSource="{Binding Attendances}"
          AutoGenerateColumns="False"
          MaxHeight="380">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Attendance ID"
                            Binding="{Binding AttendanceID}" />

                <DataGridTextColumn Header="Employee ID"
                            Binding="{Binding EmployeeID}" />

                <DataGridTextColumn Header="Date"
                            Binding="{Binding AttendanceDate, StringFormat=d}" />

                <DataGridTextColumn Header="Check In"
                            Binding="{Binding CheckIn}" />

                <DataGridTextColumn Header="Check Out"
                            Binding="{Binding CheckOut}" />

                <DataGridTextColumn Header="Total Hours"
                            Binding="{Binding TotalHours}" />

                <DataGridTextColumn Header="Status"
                            Binding="{Binding Status}" />

                <!-- Actions -->
                <DataGridTemplateColumn Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.EditAttendanceCommand,
                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}"
                            Style="{StaticResource actionButton}"
                            Background="#3a9bdc">
                                <icon:PackIconMaterial Kind="AccountEdit"
                                               Style="{StaticResource actionButtonIcon}"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>


    </StackPanel>
</UserControl>
