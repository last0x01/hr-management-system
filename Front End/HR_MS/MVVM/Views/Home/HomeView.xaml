<UserControl x:Class="HR_MS.MVVM.Views.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HR_MS.MVVM.Views.Home"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">

        <!-- Header -->
        <TextBlock Text="Dashboard" FontSize="22" FontWeight="SemiBold" 
                   Foreground="#394867" Margin="0 0 0 20"/>

        <Grid Margin="0,40,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Info Cards -->
            <StackPanel Grid.Column="0" VerticalAlignment="Top" >

                <!-- Employees Card -->
                <Border Margin="10"  Background="#5bc3ff" Height="80" CornerRadius="10" ToolTip="Total Employees">
                    <StackPanel Margin="10, 0,0,0">
                        <TextBlock Text="Employees" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding EmployeeCount}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <!-- Present Card -->
                <Border Margin="10"  Background="#2ecc71" Height="80" CornerRadius="10" ToolTip="Total Present">
                    <StackPanel Margin="10, 0,0,0">
                        <TextBlock Text="Present Today" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding PresentCount}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <!-- Late Card -->
                <Border Margin="10"  Background="#f39c12" Height="80" CornerRadius="10" ToolTip="Total Late">
                    <StackPanel Margin="10">
                        <TextBlock Text="Late Today" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding LateCount}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <!-- Absent Card -->
                <Border Margin="10"  Background="#e74c3c" Height="80" CornerRadius="10" ToolTip="Total Absent">
                    <StackPanel Margin="10">
                        <TextBlock Text="Absent Today" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding AbsentCount}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

            </StackPanel>

            <!-- Attendance Chart -->
            <Border Grid.Column="1" CornerRadius="20" Background="#f5f5f5" Padding="10">
                <oxy:PlotView Model="{Binding AttendancePlotModel}" />
            </Border>

        </Grid>

    </Grid>

</UserControl>
