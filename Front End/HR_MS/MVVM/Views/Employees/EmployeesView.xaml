<Window x:Class="HR_MS.MVVM.Views.Employees.EmployeesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HR_MS.MVVM.Views.Employees"
        mc:Ignorable="d"
               Title="Employees List" Height="600" Width="1200" WindowStartupLocation="CenterScreen">
    <Grid>

        <DataGrid ItemsSource="{Binding Employees}"
                  SelectedItem="{Binding SelectedEmployee}"
                  AutoGenerateColumns="False"
                  Grid.Column="0"
                  Margin="10,50"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  CanUserAddRows="False" Background="White" Foreground="Black"
                  >

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Person.ID}" Width="60"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding Person.FirstName}" Width="100"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding Person.LastName}" Width="100"/>
                <DataGridTextColumn Header="Age" Binding="{Binding Person.Age}" Width="60"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Person.Email}" Width="200"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Person.Phone}" Width="120"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Person.Gender}" Width="80"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Person.Address}" Width="100" />

                <DataGridTextColumn Header="Salary" Binding="{Binding Salary}" Width="100"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Job_Position}" Width="150"/>
                <DataGridTextColumn Header="Department" Binding="{Binding DepartmentID}" Width="80"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Window>
