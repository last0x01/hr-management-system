<Window x:Class="HR_MS.MVVM.Views.Logins.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HR_MS.MVVM.Views.Logins"
        xmlns:vm="clr-namespace:HR_MS.MVVM.ViewModels.Logins"
        xmlns:conv="clr-namespace:HR_MS.MVVM.Converters"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
           Background="Transparent"
         WindowStartupLocation="CenterScreen"
        Title="Login View" Height="450" Width="800">

    <Window.DataContext>
        <vm:LoginViewModel />
       
    </Window.DataContext>

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
   
        <Border  Grid.Column="1" Grid.Row="1"  Background="#FFF9F8F8" >

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="*"/>

                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0, 0, 30">
                    <StackPanel.LayoutTransform>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2" />
                    </StackPanel.LayoutTransform>




                        <TextBlock  Text="Sign In"  Foreground="Black" Margin="0 60 0 10" HorizontalAlignment="Center" FontSize="20"  FontWeight="Bold"/>
                    <StackPanel Margin="0 0 0 20" Height="25" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Margin="0 0 0 0" Content="f" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="Blue" Grid.Column="0" Width="25" Height="25"  Background="Transparent" />
                            <Button Content="M" Grid.Column="1" Width="25" Height="25"  Margin="5 0"  Background="Transparent"/>
                            <Button Content="G" Grid.Column="2" Width="25" Height="25"  Margin="5 0"  Background="Transparent"/>
                            <Button Content="E" Grid.Column="3" Width="25" Height="25"  Margin="5 0"  Background="Transparent"/>
                        </Grid>

                    </StackPanel>

                    <TextBlock  FontSize="9" HorizontalAlignment="Center" Background="Transparent"  Text="Please Enter Your UserName and Password"/>

                    <TextBox Text="{Binding Username, Mode=TwoWay}"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"
                             Height="25"
                             Width="210"
                             Style="{StaticResource RoundedTextBox}" />         





                    <PasswordBox Margin="10" Style="{StaticResource RoundedPasswordBox}"
                                x:Name="PassBox"
                                 Width="210" 
                                 Height="25"
                                 />


                 

                    <Button Command="{Binding SignInCommand}"
                            CommandParameter="{Binding ElementName=PassBox}"
                            Style="{StaticResource actionButton}"
                            Background="#FF381761" Height="25"
                            Foreground="White"
                            Width="90" Margin="20" 
                            Content="sign In"
                            FontSize="11"
                            />

                


                </StackPanel>

                <Grid Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Controls:ProgressRing 
            Width="60" 
            Height="60" 
            IsActive="True" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"/>
                </Grid>

                <Border Grid.Column="1" 
                        CornerRadius="30 0 0 30" 
                        Background="#FF381761">

                </Border>

            </Grid>

        </Border>
    </Grid>



</Window>

